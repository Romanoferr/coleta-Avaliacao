# Supabase - backend de produção (PostgreSQL + RLS).
# Copie para `.env.local` e preencha (nunca commite valores reais):
#   copy .env.example .env.local   (Windows)
#   cp .env.example .env.local     (macOS/Linux)
#
# Onde obter (passo a passo em docs/SUPABASE.md):
#   Supabase Dashboard → Project Settings → Data API

# URL pública do projeto. Ex.: https://xyzcompany.supabase.co
VITE_SUPABASE_URL=

# Chave ANON (pública, "Publishable"/anon key). É a ÚNICA chave que o frontend usa.
# NUNCA coloque aqui: service_role key, database password, JWT secret.
VITE_SUPABASE_ANON_KEY=

# Sem essas duas variáveis, o app roda em modo local (localStorage),
# com o mesmo comportamento de antes. Útil antes de configurar o Supabase
# e como fallback de desenvolvimento.

# URL pública da aplicação (usada como redirect do Auth: confirmação de
# cadastro e recuperação de senha). Sem ela, usa window.location.origin.
# Ex. dev: http://localhost:5173 - Ex. prod: https://seu-dominio.com
# IMPORTANTE: cadastre `VITE_APP_URL/#/reset-password` e
# `VITE_APP_URL/#/login` em Authentication → URL Configuration →
# Redirect URLs no Supabase Dashboard.
VITE_APP_URL=

# Otimizador de Rotas (opcional - sem chave, sem custo).
# Padrões: demo pública do OSRM e Nominatim. Só defina para auto-hospedar
# (ex.: sua instância OSRM) ou testes locais:
# VITE_OSRM_BASE_URL=https://router.project-osrm.org
# VITE_NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org

# Billing Asaas (MVP: Pro R$ 39,90, 1o mes R$ 9,90, link hospedado).
# Frontend usa apenas esta flag. Chave do Asaas fica em secrets do Supabase.
VITE_BILLING_ENABLED=1
# Secrets (supabase secrets set, nunca commitar):
# ASAAS_API_KEY, ASAAS_BASE_URL=https://api-sandbox.asaas.com/v3,
# ASAAS_WEBHOOK_TOKEN, APP_BASE_URL
